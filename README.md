# Tailwindcss Plugin Template

## Usage

Just edit ```.css``` files in ```src/styles``` folder, 
and build plugin with command ``` npm run build ```


Also, you can run in dev mode with ```npm run dev```